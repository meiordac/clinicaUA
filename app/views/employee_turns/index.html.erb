<h1>Listing employee_turns</h1>

<table>
  <tr>
    <th>Employee</th>
    <th>Turn</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employee_turns.each do |employee_turn| %>
  <tr>
    <td><%= employee_turn.employee_id %></td>
    <td><%= employee_turn.turn_id %></td>
    <td><%= link_to 'Show', employee_turn %></td>
    <td><%= link_to 'Edit', edit_employee_turn_path(employee_turn) %></td>
    <td><%= link_to 'Destroy', employee_turn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Employee turn', new_employee_turn_path %>
