<h1>Nuevo turno</h1>

<%= render 'form' %>

<% @employees.each do |user| %>
         <%= check_box_tag "user_ids[]", user.id %> 
         <%= "#{user.name}" -%><br />
      
<% end %>

<%= link_to 'Back', turns_path %>
